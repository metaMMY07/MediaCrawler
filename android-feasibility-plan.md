# MediaCrawler Android 原生可行性与任务大纲

日期：2026-09-13。状态：方案评审与验证计划，尚未交付 APK，尚无目标手机性能实测。

## 结论与范围

可以进入原生验证开发，但现阶段不能认定三平台全部满足验收。Bilibili 可作为首个交付平台；知乎、小红书都必须先通过 App 内登录和实际搜索验证。小红书是最大不确定项。先做验证 APK，再决定三平台正式产品范围。

选定 Kotlin 原生界面、端侧搜索调度、系统 WebView 官方登录、原生网络请求。用户最新确定 UI 使用 Material You，取代此前轻量 Views 的选型；实现默认建议 Jetpack Compose + Material 3，动态配色、深浅主题与无动态配色时的固定主题均纳入验收，具体依赖版本待工程阶段核对。网络基线采用随 APK 打包的 Cronet，不把 Google Play services 作为终端前提。首版建议 Android 10+、arm64，最终机型覆盖在开发入口确定；其他 ABI 若需支持，应自动提供对应 APK，避免让用户手工选架构。

仅包含 Bilibili、知乎、小红书的关键词搜索、结果卡片、必要摘要、来源跳转和分页。首屏不等待详情、评论、图片下载或全文补全。抖音移出范围。

用户补充前提：终端用户均已有这三个平台的账号。首次接入直接使用已有账号完成官方登录，不设计注册引导；这不等同于能读取其他 App 的登录态。B站仍可匿名搜索，知乎/小红书在本 App 内建立会话。

用户提供的上游 d6f7c5b、fork 8773e47、源码调用链、840 passed / 2 skipped 与 x86 测量作为已知输入，不重新审计、不把它们冒充 Android 结果。当前工作区起初只有 .git，没有可直接改造的 Android 工程。

已排除的 Termux/proot、PC/局域网后端、完整 Linux 用户态、云代抓、扩展同步不重开。另不选全站 WebView 聚合作为主路径：会把页面启动、渲染和页面结构变化放入搜索链路。它只作为条件降级。

## 每平台判断

| 平台 | 当前分级 | 依据与实现方向 | 升级为已验证的门槛 |
|---|---|---|---|
| Bilibili | 可行，Android 尚待实测 | 用户已确认 light_api 无需登录、WBI 是纯算法；将参数排序、编码、混合密钥和 MD5 移植到 Kotlin | 固定向量与原实现一致；真机匿名首屏、分页、密钥失效重取通过；蜂窝与家宽均有成功记录 |
| 知乎 | 需验证，中等风险 | 用户已确认 d_c0 + z_c0 和独立 JS 签名路径；运行 JS 不必绑定 Node 进程 | WebView 内登录拿到可用于目标请求的有效会话；JS 依赖适配完成；首屏、分页与会话恢复实测成功 |
| 小红书 | 需验证，高风险 | 用户已确认 xhshow 可在 Python 签名；这只证明现有实现存在，不证明 Kotlin 可移植、也不证明页内函数可调用 | 先通过 V1，再做 xhshow 最小依赖与确定性向量审计；原生签名或 V5 至少一条在真机可重复成功 |
| 小红书要求始终零交互且始终秒内返回 | 不建议承诺 | 验证码、登录撤销、服务端响应和页面变化不由客户端控制 | 不作为无条件产品承诺 |

现有 Python/桌面测试不能给移动端分级“盖章”。许可采用用户已给出的限制，保留移植来源与依赖许可清单；重写语言本身不作为解除派生关系的依据。

## 原生架构与关键边界

搜索页 → SearchCoordinator → 三个 PlatformAdapter → SessionStore / Signer / Transport → 平台 HTTPS。

- SearchCoordinator 并发启动已就绪的平台，结果逐平台呈现；旧查询取消，结果以 queryId 隔离。单平台错误不拖住其他平台。
- 结果缓存沿用 90 秒默认值，键包含平台、账号身份、查询、排序、筛选、页码和适配器版本；账号切换清理对应缓存。缓存结果显示时间，缓存命中与新鲜联网计时分列。
- Bilibili 使用 Kotlin WBI；首次缺少密钥可能多一次请求，单独计时。已缓存密钥不能当作永久有效。
- 知乎先在隔离的本地 WebView 页面运行打包 JS，验证编码、全局对象、模块和加密依赖；不能假设 Node 的 require/Buffer 等在 WebView 存在。注入一次、重复调用，不每页新建运行环境。
- 知乎性能阶段优先评估 AndroidX JavaScriptEngine：无须创建 WebView 视图，但它是进程外执行环境，API 26+ 且依赖 WebView 实现支持，必须 isSupported 与特性检测；不支持则保留本地 WebView 路径并单列首次初始化延迟。[官方说明](https://developer.android.com/develop/ui/views/layout/webapps/jsengine)
- 原生界面首帧不等待 CookieManager、Cronet、JS 环境或网络。依赖在首帧后按需初始化。进程被回收后首次搜索不能冒称“热态”；不能靠后台常驻三份 WebView 隐藏冷成本。
- 小红书先移植经审计的最小签名依赖；若需大量浏览器环境模拟，进入 V5 或收缩范围，不无限追逐兼容层。

Cronet 是 Chromium 网络栈，但官方资料没有保证与任一版本 Chrome 的完整 TLS/HTTP 指纹相同。版本、构建配置、协议和客户端行为仍会造成差异；“一定与 Chrome 一致”不能写进验收前提。[Cronet 官方说明](https://developer.android.com/develop/connectivity/cronet)

无 GMS 设备采用 bundled Cronet。Android 官方目前给其约 8 MB 的体积参考；这是库级参考，不是本 APK 实测，也不是所有 ABI 和构建版本的固定值。只依赖 GMS 会破坏覆盖条件。系统 HttpEngine 可以作为后续适配优化，不作为首版必备能力。[网络栈比较](https://developer.android.com/media/media3/exoplayer/network-stacks)

CookieManager.getCookie(url) 返回请求 Cookie 字符串，不是完整 Chrome Cookie 对象，不能据此还原 domain/path/expiry/SameSite 等元数据，也不能直接喂现有扩展同步接口。原生方案不经过 sync-ticket 后端。按实际目标 URL 读取 cookie，处理重复名称与作用域；响应 Set-Cookie 按响应 URL 写回 CookieManager，并等待完成后更新内存会话，显式处理重定向。[CookieManager API](https://developer.android.com/reference/android/webkit/CookieManager)

WebView cookie 存储作为持久来源；原生只保留必要的内存快照。若为了冷搜索另存凭证，采用 Keystore 保护的加密存储、禁止进入备份与日志；不能宣称 WebView 的整个存储自动受同一层加密。首版每平台一个账号，切号与退出必须清理该平台适用的 cookie 和快照，避免用全局清 cookie 影响其他平台。

## 登录与恢复的用户操作

Bilibili 匿名搜索：0 步登录。知乎、小红书首次使用：每平台仅增加 1 次 App 自有操作——点击“登录该平台”，然后在 App 内官方页面完成登录；成功验证后自动回原查询，不增加“导出 / 同步 / 验证”按钮。

短信登录的目标正常路径按逻辑动作计为 6 步：①点击登录平台；②输入手机号；③勾选官方协议（页面要求时）；④点击获取验证码；⑤输入验证码；⑥点击登录。若需切换短信登录则 +1；验证码、协议变化等额外动作单独记录。官方页面实际有几步尚未验证，不能把 6 步写成三平台实测结果。两平台共需各完成一次官方登录，不能共享身份；不请求读取全量短信，不使用需要第二台设备扫码的默认路线。

登录成功判据：官方页面完成 + 所需 cookie 可按正确 URL 读取 + 一次轻量账号/授权验证成功 + 一次搜索成功；仅存在 cookie 名称不足以判定成功。

正常会话下，每次搜索的登录附加步骤为 0。失效时流程：

1. 识别明确登录失效后暂停该平台，保留关键词、分页与其他平台结果。
2. 若官方会话可无交互恢复，仅做一次静默检查；不重复撞接口。
3. 确认需登录时，用户点击“重新登录”1 次，在原官方 WebView 完成必要操作，成功后自动重试原查询一次。
4. 验证码要求显示原官方页面，由用户处理；不能保证失效后永远零额外动作。

如果 WebView 内官方登录持续被拒，或强制跳官方 App且回不来有效 WebView 会话，该平台原生聚合登录门槛失败。外部浏览器/Custom Tabs 登录不能被视为可直接读取 cookie 的替代方案；只保留访问官方页面的降级入口。

Custom Tabs 使用浏览器共享状态，不属于本 App 的 WebView CookieManager；只有平台支持的授权回调才能作为另一种登录闭环，不能假设现有网页登录具备它。[Custom Tabs 官方说明](https://developer.android.com/develop/ui/views/layout/webapps/overview-of-android-custom-tabs)

## 风控和故障可观测性

| 处理方向 | 实施原则 | 记录或用户可见表现 |
|---|---|---|
| 网络栈与 UA | 记录 Cronet/WebView 版本和实际协议；采用与已验证端点相符的稳定客户端配置；移动 UA 与移动端点需要一起验证，不机械替换桌面 UA；不随机换身份 | 相同账号与网络下的成功率差异、403、挑战页面；HTTP 状态不能单独证明是指纹问题 |
| 会话来源 | 只使用本机官方 WebView 会话；请求前快照与响应 cookie 同步；不每次搜索重复登录预检 | 重定向登录、明确未登录业务码、会话验证失败、cookie 更新后恢复 |
| 请求节奏 | 用户触发；每平台默认 1 个在途搜索、合并同请求、取消旧查询、分页按需；不自动刷全站 | 429、Retry-After、频控业务码、挑战率变化；遵守退避，挑战时暂停自动请求 |
| 错误归因 | 分开记录 HTTP、业务码、解析错误、网络错误、运行环境错误 | HTTP 200 + HTML 验证页、200 + 业务错误、字段缺失、JS 异常，均不能显示成正常“无结果” |
| 网络变化 | Wi-Fi/蜂窝切换取消失效连接，幂等请求至多一次受控重试 | DNS/TLS/连接超时与平台拒绝分开；新 IP 后仅个别账号失败不能直接归因为 CGNAT |

不承诺“Cronet 消除风控”，不以持续重试代替验证码处理。日志记录阶段耗时、平台、匿名会话标识、provider 版本、失败类型；不记录密码、短信验证码、cookie 或完整敏感请求头。

本地签名页面与远程登录页分开，远程内容不获得广泛原生权限；仅放行明确 HTTPS 域名，桥通信验证来源。此处是登录实现的一部分。[WebView bridge 官方边界](https://developer.android.com/privacy-and-security/risks/insecure-webview-native-bridges)

## 性能：现有实测与可复现预算

现有实测仅有用户给出的 x86 Python import 368 ms、PyExecJS 5 次 332 ms（66 ms/次）。Android 重写消除了这些特定开销来源，不等于端到端分别必然节省这些毫秒。手机实测暂缺。

固定指标定义：冷启动从启动 Activity 到可见、可输入的原生搜索页；TTID 与可交互时间分别记录，不能只画空壳即宣布达标。冷启动首次联网另计。热态新鲜搜索从提交关键词到可用结果卡片；缓存命中单列；三平台首个结果与三平台全部首屏完成单列。官方也区分初始显示 TTID 和完整显示 TTFD。[启动计时](https://developer.android.com/topic/performance/vitals/launch-time)

下表全部为工程预算/情景估算，不是测量预测。假设 release + R8、arm64 中档手机、会话有效、无验证码、网络正常；签名与连接已热的条件仅适用于热态一行。

UI 变更说明：下表冷启动、绘制与 APK 体积预算形成于 Material You 选型之前，仅保留为历史预算。后续必须用最终 Material 3/Compose release 构建重新测量并更新，不把 12–19 MB 或 200–450 ms 当作新 UI 的预测或承诺。用户的冷启 <0.5 秒、热搜索 <1 秒、十兆级 APK 目标保持不变。

| 指标 | 数字与计算方式 | 限制 |
|---|---|---|
| 冷启动可交互搜索页 | 200–450 ms 预算；目标 <500 ms | 要用进程冷启动测试；JS/WebView/CookieManager 初始化阻塞主线程会使预算失效 |
| 热态缓存命中 | 20–80 ms 预算 | 不代表新鲜取数速度；记录缓存时间与是否有效 |
| 热态单平台新鲜结果 | 示例：排队 0–20 + 签名 1–30 + 网络/服务端 200–700 + 解析绘制 20–60 = 221–810 ms | 输入是待测假设；无线网络和服务端长尾可超过 1 秒 |
| 三平台新鲜结果 | 首个约 min(T_b,T_z,T_x)，全部约 max(T_b,T_z,T_x)，另计调度/绘制与资源竞争 | 不能用最快 B 站代替“三平台搜索 <1s”验收 |
| 冷态首次新鲜搜索 | T_运行环境初始化 + T_cookie恢复 + T_密钥/会话请求 + T_搜索；并行部分按关键路径计算 | 尚无足够证据给窄区间；即使 UI <500 ms，也不能据此宣称首次取数 <500 ms |
| arm64 APK | 原生业务/资源 3–7 MB + Cronet 约 8 MB + 签名/其他依赖 1–4 MB，合计约 12–19 MB | 粗估、不是构建输出；多 ABI 通用包和资源可能增大。目标先定 ≤20 MB |
| 安装 | 已下载 APK：读写 15–30 MB / 50–150 MB/s ≈ 0.1–0.6 秒，再假设校验/编译/系统处理 1–5 秒，总约 1.1–5.6 秒 | 模型参数需真机替换；OEM 安装扫描可能显著更久，不能保证所有机型 |
| 下载 | 15 MB × 8 / 20 Mbps ≈ 6 秒；100 Mbps ≈ 1.2 秒 | 理论传输下限，未计握手和重传；不能包含在“安装 2 秒”口径内 |

侧载首次还可能需要允许该安装来源、确认安装及系统检查，单独录像计步；这与“登录之外正常搜索零额外步骤”应分开统计。

验收暂以冷启可交互与热态新鲜搜索的 p95 是否达到 0.5 秒 / 1 秒判定，同时报告 p50、p95、样本量、失败数。普通蜂窝/可正常联网 Wi-Fi 必须均可使用，但任意网络不具备有界时延，弱网、门户认证和断网作为故障恢复测试，不承诺秒内完成。

## 分阶段任务与退出门槛

工期只作排期估算，以一名主开发加有限委派为基础；不含等待真实账号/手机、平台放行或外部许可处理。失败门槛出现后停止该平台扩建。

| 阶段 | 任务及优先级 | 产物与验收 | 估算 |
|---|---|---|---|
| T0 | 确认目标手机/系统/WebView/无 GMS 覆盖；固定源码与依赖版本；建立 Material You 原生项目与测量口径 | 可安装 release 验证壳、Material 3 深浅主题/配色回退、版本清单、原始计时格式；默认范围假设明确 | 0.5–1 工程日 |
| T1 | P0 / V1：小红书、知乎 WebView 登录与 cookie 闭环，B 站匿名基线 | 各平台官方登录录像、实际步骤、cookie 名称/作用域摘要（无值）、验证/搜索记录；重启/杀进程/切网后复测 | 1–2 工程日 |
| T2 | P0 / V2：B站 WBI、知乎本地 JS、xhshow 最小依赖审计与试移植 | 固定输入向量、Android 与基线比对、首屏和分页请求；编码覆盖中文、emoji、空值、特殊符号；时间/随机源受控。建议每 signer ≥100 个离线向量，随机性部分按协议不变量比较 | 2–4 工程日 |
| T3 | P0 条件触发 / V5：小红书签名路径失败即验证官方页内搜索 | 同源实际取数/渲染、分页、字段完整性、挑战处理、首次页面加载、热重复、renderer 重启恢复；每项有成功或失败证据。JS 能执行不等于站点函数可调用 | 1–2 工程日，严格时间盒 |
| T4 | P1 / V3：蜂窝与家宽风控对照 | 同手机/账号/查询集合分时交叉对照；增加桌面同家宽、桌面经手机热点作为辅助，记录设备差异等混杂；每平台每路径每网络建议 ≥30 次正常人工节奏查询，分散到 ≥3 天；不把小样本零挑战解释为绝对稳定 | 2–3 工程日，可与集成穿插 |
| T5 | Material You 聚合 UI、会话恢复、缓存、取消/分页和独立错误状态 | B站先可用；通过门槛的平台再接入。搜索框、平台筛选、结果卡片与账号入口统一 Material 3；未登录、验证码、限流、网络异常、签名失败、真实空结果能分别显示；主代理目视验收 | 2–3 工程日 |
| T6 | P1 / V4：目标手机性能、内存、电耗、安装验收 | 每机 release 冷启 ≥30 次；各平台/网络热态新鲜 ≥50 次，分时正常节奏采集；缓存样本独立。APK 字节数/SHA256、安装录像 ≥5 次、Perfetto、主进程+WebView renderer 总 PSS、固定亮度/相同网络的 15 分钟搜索脚本与空闲对照耗电 | 1–2 工程日 |
| T7 | 发布候选与回归 | 最终 APK、兼容性矩阵、逐平台分级、已知问题、基准原始文件和报告、来源/许可记录；用户无 PC/Termux/root 完整走通 | 0.5–1 工程日 |

T0→T1 是首先执行的关键路径。T2 的 B 站和离线签名工作可以与 T1 独立进行；T3 在失败条件触发时尽早插入，不能等产品做完才验证。总体约 10–18 工程日，条件降级与平台阻断会改变范围；不是三平台必成承诺。

V6：正式 APK 不包含 Python/PyExecJS，因此端侧不适用。若离线向量生成继续用 Python，测试工具明确 PYTHONUTF8=1，并在已复现的 cp1252 环境做一次回归；它不成为手机依赖。

## 小红书降级决策

1. Kotlin 签名成功且稳定：保持完整原生聚合结果与正常分页，除登录/挑战外无额外操作。
2. 移植失败，但 V5 官方页内自动取数成功：关键词提交仍为 1 次；B站/知乎先出结果，小红书显示独立加载后补卡片。仅提供实测拿到的搜索结果字段、链接和分页；无法稳定取到的详情、评论与全文补全关闭。记录更高的页面启动延迟、内存、流量与耗电，不能宣称仍满足 <1 秒。
3. 无法稳定结构化取数，但官方页面可用：小红书退化为 App 内“网页结果”入口；尝试自动带入关键词，失败时需要用户在官方页面再输入搜索。没有统一卡片、跨平台排序、结构化摘要、统一分页；网页阻拦或登录动作仍由用户完成。
4. 官方 WebView 登录/页面本身也不可用：默认聚合只保留 B站与通过验证的知乎，小红书禁用并保留可选官方跳转。外部跳转会离开 App，明确它不满足原生聚合验收，不视为完成三平台交付。

知乎若登录门槛失败，适用同样的网页入口/禁用原则；不能因为签名移植简单就默认登录可用。

## 执行分工

主代理负责形态选择、登录闭环判断、签名路径取舍、真机画面验收、性能结论与最终范围决定。Luna xhigh 可负责独立官方资料核对、离线向量与限定文件的辅助工作；ask_dsh 负责有明确目录/输入/产物/验收的构建、lint、单测和日志整理。独立任务才并行，报告必须经过复核，画面由主代理或用户验收。

首次工程里程碑是“真机登录验证 APK + B站首屏”，不先把 Python 引擎装进 APK，也不先堆满三平台 UI。

环境盘点补充：已发现 JDK、SDK、Gradle，主代理复核 JDK 可运行；当前 adb 只有模拟器，无在线真机。尚未实际构建验证依赖与版本组合。环境复核详见同目录 android-environment-audit.md；真机测试 APK 可以直接侧载，终端用户无须 ADB。
