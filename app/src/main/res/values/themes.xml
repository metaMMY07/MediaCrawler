<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Platform theme only: the UI itself is fully Compose Material3 (Material You),
         so no AppCompat / Material-Components dependency is required. -->
    <style name="Theme.MediaSearch" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/app_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>
</resources>
